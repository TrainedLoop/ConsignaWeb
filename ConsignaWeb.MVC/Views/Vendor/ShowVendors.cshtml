@using ConsignaWeb.MVC.Models.Repository;
@{
    List<Users> vendors = ViewBag.vendors;
}
@section scripts
{
    @Scripts.Render("~/scripts/tables")
    @Styles.Render("~/stylesheets/tables")
    <script>
        $(document).ready(function () {
            $('#tablevendor').dataTable(
                {
                    "oLanguage": {
                        "sProcessing": "Aguarde enquanto os dados são carregados ...",
                        "sLengthMenu": "Mostrar _MENU_ registros por pagina",
                        "sZeroRecords": "Nenhum registro correspondente ao criterio encontrado",
                        "sInfoEmtpy": "Exibindo 0 a 0 de 0 registros",
                        "sInfo": "Exibindo de _START_ a _END_ de _TOTAL_ registros",
                        "sInfoFiltered": "",
                        "sSearch": "Procurar",
                        "oPaginate": {
                            "sFirst": "Primeiro",
                            "sPrevious": "Anterior",
                            "sNext": "Próximo",
                            "sLast": "Último"
                        }
                    }
                });
        });

        //$(function () {
        //    // run the currently selected effect
        //    function runShowVendorEffect() {
        //        // get effect type from
        //        var selectedEffect = $("Slide");

        //        // most effect types need no options passed by default
        //        var options = {};
        //        // some effects have required parameters
        //        if (selectedEffect === "scale") {
        //            options = { percent: 100 };
        //        } else if (selectedEffect === "size") {
        //            options = { to: { width: 280, height: 185 } };
        //        }

        //        // run the effect
        //        window.location.hash = '#ShowVendorEffect';
        //        $("#ShowVendorEffect").show(selectedEffect, options, 500);
        //    };

        //    //callback function to bring a hidden box back
        //    function callbackShowVendorEffect() {
        //        setTimeout(function () {
        //            $("#ShowVendorEffect:visible").removeAttr("style").fadeOut();
        //        });
        //    };

        //    // run effect
        //    $("#openVendor").click(function () {
        //        var buttonPress = $("#openVendor").val();
        //        if (buttonPress == "notPress") {
        //            runShowVendorEffect();
        //            $("#openVendor").val("press");
        //            $("#openVendor").attr("src", "/content/Icos/red-x-icon.png");
        //        }
        //        else {
        //            callbackShowVendorEffect();
        //            $("#openVendor").val("notPress");
        //            $("#openVendor").attr("src", "/content/Icos/blue-plus-icon.png");
        //        }

        //        return false;
        //    });

        //    $("#ShowVendorEffect").hide();
        //});

    </script>
}
<div class="col-md-12">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">Lista de vendedores</div>
            <div class="panel-body">
                <table class="table table-striped table-bordered table-hover" id="tablevendor">
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th>S.Nome</th>
                            <th>Email</th>
                            <th>Doc</th>
                            <th>Editar</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in vendors)
                        {
                            <tr>
                                <td>@item.Name</td>
                                <td>@item.SurName</td>
                                <td>@item.Email</td>
                                <td>@item.CPF</td>
                                <td><input type="button" onclick="location.href='/Vendor/Edit?vendorid=@item.Id'" class="btn btn-default btn-sm" value="Editar" /></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>